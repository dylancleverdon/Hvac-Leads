# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
pubspec.lock

# Android
android/app/src/main/java/io/flutter/plugins/
android/.gradle/
android/captures/
android/gradlew
android/gradlew.bat
android/local.properties
android/**/*.iml
android/app/release/
*.jks
*.keystore
# CI's persistent release-signing key is intentionally committed (see
# .github/workflows/release.yml) so every release builds with the same
# signature and the in-app updater can install in place.
!android/app/ci-release.keystore

# IDE
.idea/
.vscode/
*.iml

# macOS
.DS_Store
